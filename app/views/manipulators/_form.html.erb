<%= form_for(@manipulator) do |f| %>
  <% if @manipulator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manipulator.errors.count, "error") %> prohibited this manipulator from being saved:</h2>

      <ul>
      <% @manipulator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :m_name %><br>
    <%= f.text_field :m_name %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :m_type %><br>
    <%= f.number_field :m_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
